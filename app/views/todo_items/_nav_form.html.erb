<%= form_with(model: @todo_item) do |f| %>
  <% if @todo_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todo_item.errors.count, "error") %> prohibited this todo_item from being saved:</h2>

      <ul>
        <% @todo_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class='form-inline my-2 my-lg-0'> 
    <%= f.text_field :body, class: 'form-control mr-sm-2' %>
    <%= f.submit 'Add Todo Item', class: 'btn btn-outline-success' %>
  </div>
<% end %>